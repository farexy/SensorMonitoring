using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using SensorMonitoring.BusinessLogin.SensorReadingServiceReference;

namespace SensorMonitoring.BusinessLogin.BL
{/*
    public class SensorReadingLogic : IBaseLogic<>
    {
        private SensorReadingServiceReference.DataServiceOf_SensorReadingDtoClient serviceOfSensorReading =
            new DataServiceOf_SensorReadingDtoClient();

        public SensorReadingServiceReference. Get(object[] keys)
        {
             d = serviceOfSensorReading.GetItem(keys);
            return serviceOfSensorReading.GetItem(keys);
        }

        public void Create(SensorReadingDto sensor)
        {
            serviceOfSensorReading.AddItem(sensor);
        }

        public void Update(SensorReadingDto sensor)
        {
            serviceOfSensorReading.AddItem(sensor);
        }

        public void Delete(object[] keys)
        {
            //serviceOfSensor.
        }

        public bool IsValid(string name, string password)
        {
            string decryptPassword = Encrypter.Decrypt(password);
            return true;
        }
    }*/
}